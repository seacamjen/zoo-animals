import { Componenet, Input, Output, EventEmitter } from '@angular/core';
import { Animal } from './zoo-animals.model';

@Componenet({
  selector: `animal-list`,
  template: `
  
  `
})

export class AnimalListComponent {

}
